export const useVisitorTracker = () => {
    // Phase 0 intentionally disables anonymous database writes. A later analytics
    // phase will replace this with a rate-limited, privacy-aware server endpoint.
};
